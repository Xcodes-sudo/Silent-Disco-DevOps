apiVersion: 1

datasources:
  - name: Graphite
    type: graphite
    access: proxy
    url: http://graphite-server:80
    jsonData:
      graphiteVersion: "1.1"
    isDefault: true
    editable: true
