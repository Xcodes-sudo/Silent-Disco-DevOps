apiVersion: v1
kind: Service
metadata:
  name: silent-disco-service
  labels:
    app: silent-disco
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
  selector:
    app: silent-disco
